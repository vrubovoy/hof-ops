---
# network.ensure - a named, Hof-labeled Docker network (also used to repair a baseline-expected network gone missing, see plan.mjs's own computeMissingResources).
# Real implementation (PR #28/#29): community.docker.docker_network, idempotent, same label set as volume.ensure.
hof_operation_id: null
hof_network_name: null
