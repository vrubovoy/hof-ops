---
# secret.ensure - delivers one named secret's plaintext to its own fixed target-side file path (/etc/hof/secrets/<name>, see render-topology.mjs's own wireSecret()).
# Real implementation (PR #28/#29): reads the decrypted value hofctl apply already resolved (never decrypts SOPS itself - that's the control plane's own job, see scripts/secrets.mjs), writes it to the fixed path with 0400 permissions, owned by the Compose-invoking user.
hof_operation_id: null
hof_secret_name: null
