{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hof.dev/schemas/service-catalog-v1.schema.json",
  "title": "Hof service catalog",
  "type": "object",
  "additionalProperties": false,
  "required": ["apiVersion", "services"],
  "properties": {
    "apiVersion": { "const": "hof.dev/catalog/v1" },
    "services": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/service" }
    }
  },
  "$defs": {
    "identifier": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]{0,62}$"
    },
    "service": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "mandatory", "hostname", "artifacts", "dependsOn", "volumes", "health"],
      "properties": {
        "id": { "$ref": "#/$defs/identifier" },
        "mandatory": { "type": "boolean" },
        "hostname": {
          "type": ["string", "null"],
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "artifacts": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/identifier" }
        },
        "dependsOn": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/identifier" }
        },
        "volumes": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/identifier" }
        },
        "health": {
          "type": "object",
          "additionalProperties": false,
          "required": ["component", "path"],
          "properties": {
            "component": { "$ref": "#/$defs/identifier" },
            "path": { "type": "string", "pattern": "^/" }
          }
        }
      }
    }
  }
}
